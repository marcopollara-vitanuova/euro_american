import { notFound } from "next/navigation";
import { AppShell } from "@/components/layout/app-shell";
import { Card, CardHeader } from "@/components/ui/card";
import { customers, demoDocuments, policies, quotes } from "@/lib/demo/data";
export default async function CustomerDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const customer = customers.find((item) => item.id === id); if (!customer) notFound(); return <AppShell title={customer.displayName} breadcrumb="Cliente / dettaglio"><div className="data-grid"><Card><CardHeader title="Anagrafica" /><dl className="grid gap-2 text-sm"><div><dt className="text-slate-500">Email</dt><dd>{customer.email}</dd></div><div><dt className="text-slate-500">Codice fiscale</dt><dd>{customer.taxCode}</dd></div><div><dt className="text-slate-500">Città</dt><dd>{customer.city} ({customer.province})</dd></div></dl></Card><Card><CardHeader title="Storico" /><p>Preventivi: {quotes.filter((q) => q.customerId === id).length}</p><p>Polizze: {policies.filter((p) => p.customerId === id).length}</p><p>Documenti: {demoDocuments.filter((d) => d.entityId === id).length}</p><p className="mt-3 text-sm text-slate-500">Audit modifiche, note interne, duplicati e data retention predisposti.</p></Card></div></AppShell>; }
