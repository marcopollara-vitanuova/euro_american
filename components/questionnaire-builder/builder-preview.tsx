import { questionnaires, products } from "@/lib/demo/data";
export function QuestionnaireBuilderPreview() { const questionnaire = questionnaires[0]; const product = products.find((item) => item.id === questionnaire.productId); return <div className="card p-5"><h2 className="text-xl font-bold">Builder questionario</h2><p className="mt-1 text-sm text-slate-500">Bozza configurabile per {product?.name}</p><div className="mt-4 space-y-4">{questionnaire.sections.map((section) => <section key={section.code} className="rounded-2xl border border-slate-200 p-4"><h3 className="font-bold">{section.title}</h3>{section.questions.map((question) => <div key={question.code} className="mt-3 rounded-xl bg-slate-50 p-3 text-sm"><strong>{question.code}</strong> · {question.label}<div className="text-slate-500">type={question.fieldType} required={String(question.required)}</div></div>)}</section>)}</div></div>; }
